<?php
use NetglueMoney\Money;

$m = new Money(100, 'USD', time());

//var_dump($this->cc);

//var_dump($this->cc->getAdapter()->getSupportedCurrencyCodes());

$amount = 100;

$from = 'USD';

$out = array(
	'GBP',
	'ZAR',
	'BWP',
	'EUR'
);

foreach($out as $to) {
	var_dump($this->cc->convertMoney($m, $to));
}

